platform :ios, '9.0'
use_frameworks!

target 'aspace' do

    pod 'Mapbox-iOS-SDK', '~> 4.4'
    pod "SHSearchBar"

    target 'aspaceTests' do
        inherit! :search_paths
        # Pods for testing
    end

    target 'aspaceUITests' do
        inherit! :search_paths
        # Pods for testing
    end
end
